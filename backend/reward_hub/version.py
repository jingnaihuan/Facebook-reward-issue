# -*- coding: utf-8 -*-
"""发奖中台版本号——唯一来源。

- server 通过 /api/ping 回传，供启动器判断线上后台是否为当前版本；
- 启动器 [[app_entry]] 据此决定「复用旧后台」还是「杀旧重起」（修复更新 exe 后旧后台仍在、
  新版被静默沿用的问题）；
- 前端页脚显示，便于一眼确认跑的是不是新版。

发版时改这里一处即可。
"""
VERSION = "1.9"
